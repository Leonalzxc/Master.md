CODER_RESULT:

Нужен ещё один последний разведзапуск — без вывода миграций и реальных ошибок `tsc` я добавлю поля наугад. Запускаю минимальный набор команд (без обрезки), чтобы точно увидеть SQL-схему и список ошибок типов.

### RUN
```sh
cd web
git checkout fix/types-and-eslint 2>/dev/null || git checkout -b fix/types-and-eslint
echo "===PWD==="; pwd
echo "===TREE supabase==="
find supabase -type f 2>/dev/null
echo "===MIGRATIONS==="
for f in supabase/migrations/*.sql; do
  echo "######## $f ########"
  cat "$f"
done
echo "===END MIGRATIONS==="
echo "===types.ts FULL==="
wc -l src/lib/supabase/types.ts
sed -n '1,400p' src/lib/supabase/types.ts
echo "===ESLINT FILES==="
ls -la eslint.config.* .eslintrc* 2>/dev/null
echo "---eslint.config.mjs---"
cat eslint.config.mjs 2>/dev/null
echo "---eslint.config.js---"
cat eslint.config.js 2>/dev/null
echo "===package.json==="
cat package.json
echo "===TSC ERRORS==="
npx tsc --noEmit 2>&1 | head -80
echo "===LINT ERRORS==="
npx next lint 2>&1 | head -60
echo "===GREP urgent/area in src==="
grep -rn "\.urgent\|\.area\b\|status: \"active\"\|=== \"active\"" src/ 2>/dev/null | head -40
```

CODER_QUERY: Менеджер, твоё прошлое сообщение обрезало вывод `types.ts` на середине и **не содержало вообще миграций, ESLint-конфига и ошибок tsc/lint** — именно того, ради чего я просил запуск. Без этих данных любые мои правки будут гаданием: я могу:

- добавить `urgent: boolean` и `area: string | null` в `Job`, но в схеме БД поля могут называться иначе (`is_urgent`, `area_id`),
- сменить `JobStatus` на `"active" | …`, но статусы в БД могут быть совсем другими,
- переписать `eslint.config.mjs`, но не вижу что именно вызывает цикл.

Пожалуйста, выполни RUN-блок выше и пришли **полный, необрезанный** вывод (особенно `######## migrations ########`, `===TSC ERRORS===`, `===LINT ERRORS===`). После этого — обещаю — в следующем ответе будут только финальные блоки файлов и коммит/пуш в `fix/types-and-eslint`, без новых вопросов.